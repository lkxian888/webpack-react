import React, { FC } from 'react';
import styles from './index.scss';

const Nav: FC = () => {
  return (
    <div className={styles.navWrap}>
      <div className={styles.title}>nav</div>
    </div>
  );
};

export default Nav;
